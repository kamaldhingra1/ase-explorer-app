# ASE v2.0.1.2 - Custom Domain SEO Hotfix

## Included

- Replaced ASE GitHub Pages canonical/Open Graph/sitemap/robots URLs with `https://ase.kamaldhingra.com/`.
- Added `CNAME` with `ase.kamaldhingra.com` for GitHub Pages custom domain deployment.
- Preserved SATF GitHub repository links and all relative CSS/JS/image links.

## Deploy

For a targeted patch, deploy:

- all updated HTML files
- `sitemap.xml`
- `sitemap.html`
- `robots.txt`
- `CNAME`

Or deploy the full v2.0.1.2 package contents.
