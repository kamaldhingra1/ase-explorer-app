
export function setupMobileMapControls({scrollEl,svgEl,fitBtn,zoomInBtn,zoomOutBtn,resetBtn,mapWidth=1200,mapHeight=900,min=.42,max=1.85}){if(!scrollEl||!svgEl||!fitBtn||!zoomInBtn||!zoomOutBtn||!resetBtn)return false;let scale=1;const clamp=v=>Math.max(min,Math.min(max,v));const apply=(next,center=true)=>{scale=clamp(next);svgEl.style.width=`${Math.round(mapWidth*scale)}px`;svgEl.style.height=`${Math.round(mapHeight*scale)}px`;svgEl.style.minWidth=`${Math.round(mapWidth*scale)}px`;if(center)requestAnimationFrame(()=>{scrollEl.scrollLeft=Math.max(0,(svgEl.clientWidth-scrollEl.clientWidth)/2);scrollEl.scrollTop=Math.max(0,(svgEl.clientHeight-scrollEl.clientHeight)/2);});};const fit=()=>{const f=Math.min(scrollEl.clientWidth/mapWidth,scrollEl.clientHeight/mapHeight);apply(Math.max(.52,Math.min(.9,f)),true);};fitBtn.onclick=fit;zoomOutBtn.onclick=()=>apply(scale-.12,true);resetBtn.onclick=()=>apply(1,true);zoomInBtn.onclick=()=>apply(scale+.12,true);window.addEventListener('resize',()=>{if(matchMedia('(max-width:860px)').matches)fit();});fit();return true;}
